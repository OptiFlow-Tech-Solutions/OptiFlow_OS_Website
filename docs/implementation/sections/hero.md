# Hero Section
- **Status:** partial | **Used On:** home.html only
- **Priority:** high

## Feature IDs
- UX-004 — style extraction
- ACC-001 — aria-hidden

## Pending Improvements
- [ ] Extract inline styles to `core.css` (UX-004)
- [ ] Replace CSS mockup with real product screenshot (post product launch)

## Dependencies / Implementation Notes
Contains: typewriter animation, hook badge, highlight-gradient text, mouse-glow effect (CSS radial gradient tracking cursor), dashboard mockup (pure CSS, no images), floating widget elements, trust badges. Heavy inline `<style>` block — needs extraction to `core.css` under UX-004. The CSS-only dashboard mockup is a placeholder; real screenshot pending product launch.
