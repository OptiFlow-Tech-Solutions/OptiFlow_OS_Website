## Why

Source HTML files contain broken UTF-8 encoding artifacts (`â€"`, `â‚¹`, `Ã—`) — mojibake from UTF-8 bytes misinterpreted as Windows-1252. This causes em dashes, rupee signs, and multiplication signs to render as garbled characters in browsers, damaging the professional appearance of the OptiFlow OS website.

## What Changes

- Replace `â€"` / `â€"` (broken em dash `—`) with `&mdash;` across all source HTML pages
- Replace `â‚¹` (broken Indian rupee sign `₹`) with `&#8377;` across all source HTML pages  
- Replace `Ã—` (broken multiplication sign `×`) with `&times;` across all source HTML pages
- Verify no broken sequences remain in any source or partial file

## Capabilities

### New Capabilities

- `html-encoding-fix`: Systematic replacement of broken UTF-8 sequences with HTML entities across all 16 source pages and 3 partials, followed by validation to ensure no mojibake remains.

### Modified Capabilities

<!-- No existing specs to modify -->

## Impact

- Affected source files: `competitive-positioning.html`, `demo-booking.html`, `contact.html`, `why-optiflow.html`, `terms.html`, `faq.html`, `product-overview.html`, `feature-showcase.html` and potentially others in `src/pages/`
- Partials (`nav.html`, `footer.html`, `analytics.html`) may also need fixes
- Dist artifacts will be regenerated by the build pipeline with correct entities
- No API, dependency, or CSS changes required
- No breaking changes — all replacements are content-only, preserving semantics
